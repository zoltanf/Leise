# Baseline capture notes

- Power source: AC power
- Battery: 80%, not charging
- Other Leise processes: stopped before launch and transcription captures
- Foreground workload: minimized, but the machine was not laboratory-isolated
- Thermal state: `pmset` did not return a useful thermal pressure value for this
  capture
- Core ML cache: each downloaded Parakeet model received one excluded compiler-
  cache warm-up before its seven first-use measurements
- Cold OS cache: uncontrolled; no cold-launch value is reported
